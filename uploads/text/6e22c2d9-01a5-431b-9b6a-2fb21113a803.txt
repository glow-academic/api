## Context

Group ID: `019d8ce1-1a35-75be-b01f-785f7c741afa`.
## Parameters

Pass these to your tools:
- `test_id`: `019d8ce1-59db-7370-95c2-67441dd2919b`
- `invocation_id`: `019d8ce1-59e9-7189-ba52-a36c5e2c3e8d`
- `run_id`: `019d8ce1-59d3-7015-91da-eb6e1740953d`

## Tools

- **Test_Feedback**(grade_id, tool_call_id, standard_group_id, score, feedback) — Create a feedback entry for a rubric criterion within a test grade
- **Test_Text_Download**(text_id) — Download text content by text_id from a test run
- **Test_Call_Download**(id) — Download call content by ID from a test run
- **Test_Get**(id, start_date, end_date, eval_ids, department_ids, page, page_size, search, status, archived, sort_by, sort_order) — Get operation for Test
- **Test_Grade**(invocation_id, score) — Create a grade entry for a test invocation with overall score

## What To Do

1. Call **Test_Get** to see the rubric standard groups and the original agent's output.
2. Review the output against each standard group's criteria.
3. Call **Test_Grade** with `invocation_id`, `passed` (true/false), and `score` (sum of all standard group scores). This returns a `grade_id`.
4. For each standard group, call **Test_Feedback** with:
   - `grade_id` from step 3
   - `tool_call_id` — the specific tool call from the original output being evaluated
   - `score` — 1 to 5
   - `feedback` — specific evidence from the output

Always use tool calls. Do not output plain text.