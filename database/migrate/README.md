# Database migrate folders

The legacy SQL migration scripts under `add/` and `remove/` were intentionally deleted.

These folders are kept only to preserve repository structure for downstream tooling.
