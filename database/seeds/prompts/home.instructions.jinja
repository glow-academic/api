## Context

{% if params.artifact_id %}
**Existing home** (ID: `{{ params.artifact_id }}`).
Use `Home_Get(id: "{{ params.artifact_id }}")` to see current state.
{% else %}
**New home** — no artifact exists yet.
{% endif %}
{% if resources %}Requested resources: **{{ resources | join(', ') }}**.{% endif %}
{% if group_id %}Group ID: `{{ group_id }}`.{% endif %}

{% set draft = views.draft_home if views and views.draft_home else None %}

{% if draft %}
### Current State
{{ draft | tojson }}
{% endif %}

No additional resource context is available for this artifact type.
