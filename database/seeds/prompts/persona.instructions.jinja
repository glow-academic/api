## Context

{% if request.artifact_id %}
**Existing persona** (ID: `{{ request.artifact_id }}`).
Use `Persona_Get(id: "{{ request.artifact_id }}")` to see current state.
{% else %}
**New persona** — no artifact exists yet.
{% endif %}
{% if resources %}Requested resources: **{{ resources | join(', ') }}**.{% endif %}

## Parameters

{% if params %}
Pass these to your tools:
{% for key, value in params.items() %}
- `{{ key }}`: `{{ value }}`
{% endfor %}
{% endif %}

## Tools

{% for tool in tools %}
- **{{ tool.name }}**({{ tool.args | join(', ') }}){% if tool.description %} — {{ tool.description }}{% endif %}

{% endfor %}

## What To Do

{% if 'colors' in resources %}
Generate color values as hex codes. Create visually distinct, professional colors suitable for UI display.
{% endif %}
{% if 'names' in resources %}
Generate clear, descriptive persona names that identify the character's role or personality.
{% endif %}
{% if 'descriptions' in resources %}
Write detailed descriptions explaining the persona's background, role, and communication style.
{% endif %}
{% if 'instructions' in resources %}
Write behavioral instruction templates that define how the persona acts during training simulations. Include tone, knowledge boundaries, and response patterns.
{% endif %}
{% if 'icons' in resources %}
Select an appropriate icon identifier that visually represents the persona's role.
{% endif %}

Only generate what was asked for. Only pass fields you need.
