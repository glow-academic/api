Create an attempt_chat entry within an attempt. Pass the chat_id (template) and all resource IDs (scenarios, personas, objectives, etc.) to configure the chat.

Required: attempt_id, chat_id
Optional: all resource connection IDs (scenario_ids, persona_ids, objective_ids, etc.) and config flags (audio_enabled, text_enabled, etc.)